---
import BlogLayout from '../../layouts/BlogLayout.astro';
import { getPostsByLocale, postPath } from '../../lib/blog';
import { BLOG_INDEX_ALTERNATES, blogIndex, feedPath } from '../../lib/seo';

const posts = await getPostsByLocale('en');
const page = blogIndex('en');
---

<BlogLayout
	title={page.title}
	description={page.description}
	locale="en"
	alternates={BLOG_INDEX_ALTERNATES}
	feed={feedPath('en')}
>
	<h1>Devlog</h1>
	{posts.length === 0 && <p>No posts yet — check back soon.</p>}
	<ul class="post-list">
		{
			posts.map((post) => (
				<li>
					<a href={postPath('en', post.slug)}>{post.data.title}</a>
					<p class="meta">
						{post.data.date.toISOString().slice(0, 10)} — {post.data.description}
					</p>
				</li>
			))
		}
	</ul>
</BlogLayout>

<style>
	.post-list {
		list-style: none;
		padding: 0;
		margin: 2em 0 0;
	}
	.post-list li {
		margin-bottom: 1.75em;
	}
	.post-list a {
		font-size: 18px;
	}
	.meta {
		margin: 0.35em 0 0;
		font-size: 13px;
		color: var(--muted);
	}
</style>
