{
  "schema_version": 1,
  "_comment": "SCHEMA EXAMPLE ONLY — fake path. Real local repo paths are machine-specific and personal, so they live only in repos.local.json (gitignored, never committed). Without a repos.local.json, a scan only sees this website repo. Copy this file to repos.local.json and point it at the real game repo once it exists locally.",
  "repos": [
    {
      "id": "game",
      "label": "HoldStrong (game)",
      "path": "C:\\path\\to\\hold-strong"
    }
  ]
}
